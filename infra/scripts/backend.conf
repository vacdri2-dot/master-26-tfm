# Single source of truth for the Terraform remote backend.
# Sourced by scripts/bootstrap-backend.sh to create the backing RG,
# storage account and container, and to render environments/dev/backend.hcl.
#
# LOCATION is pinned to swedencentral because the Azure for Students (UCM)
# tenant restricts deployments by policy to: norwayeast, polandcentral,
# italynorth, germanywestcentral, swedencentral. See infra/AGENTS.md
# (Subscription Constraints) for rationale.
RESOURCE_GROUP_NAME="rg-tfm-ucm-g2-dev"
STORAGE_ACCOUNT_NAME="sttfmucmg2dev01"
CONTAINER_NAME="tfstate"
STATE_KEY="dev.terraform.tfstate"
LOCATION="swedencentral"
